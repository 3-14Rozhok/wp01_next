import Post from '../Post'

const List = () => {
    return (
        <section className="min-h-screen flex flex-col w-max min-w-full p-8">
            <Post />
            <Post />
            <Post />
        </section>
    )
}

export default List
